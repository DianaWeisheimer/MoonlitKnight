using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class WeaponStats : Stats
{
    /*public BaseStat baseStat;
    public StatModifiers modifiers;

    public WeaponStats CalculateStats()
    {
        SetBaseStats(baseStat);
        SetModifiers();

        return this;
    }

    public void SetModifiers()
    {
        if (modifiers.modifiers != null)
        {
            modifiers.CalculateModifiers();
            health.SetModifier(modifiers.healthModifier, modifiers.healthMultiplier);
            mana.SetModifier(modifiers.manaModifier, modifiers.manaMultiplier);
            stamina.SetModifier(modifiers.staminaModifier, modifiers.staminaMultiplier);
        }
    }*/
}
